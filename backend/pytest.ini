[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
log_cli = true
log_cli_level = WARNING
markers =
    integration: 실행 중인 Fuseki 인스턴스가 필요한 통합 테스트 (pytest -m integration)
addopts = -m "not integration"
